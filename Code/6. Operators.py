"""
Python divides the operators in the following groups:

Arithmetic operators
Assignment operators
Comparison operators
Logical operators
Identity operators
Membership operators
Bitwise operators
"""

# Airthematic Operators
"""
+	Addition	    x + y	
-	Subtraction	    x - y	
*	Multiplication	x * y	
/	Division	    x / y	
%	Modulus	        x % y	
**	Exponentiation	x ** y	
//	Floor division	x // y
"""

# Assignment operators
"""
=	    x = 5	    x = 5	
+=	    x += 3	    x = x + 3	
-=	    x -= 3	    x = x - 3	
*=	    x *= 3	    x = x * 3	
/=	    x /= 3	    x = x / 3	
%=	    x %= 3	    x = x % 3	
//=	    x //= 3	    x = x // 3	
**=	    x **= 3	    x = x ** 3	
&=	    x &= 3	    x = x & 3	
|=	    x |= 3	    x = x | 3	
^=	    x ^= 3	    x = x ^ 3	
>>=	    x >>= 3	    x = x >> 3	
<<=	    x <<= 3	    x = x << 3	
"""

# Comparison operators
"""
==	Equal	                    x == y	
!=	Not equal	                x != y	
>	Greater than	            x > y	
<	Less than	                x < y	
>=	Greater than or equal to	x >= y	
<=	Less than or equal to	    x <= y
"""

# Logical operators
"""
and 	True, if both	x < 5 and  x < 10	
or	    True, if one	x < 5 or x < 4	
not	    reverse	        not(x < 5 and x < 10)
"""

# Identity operators
"""
is      return True if both same.   x is y
is not  return true if not same.    x is not y
"""

# Membership operators
"""
in       return true if found in.       x in y
not in   return true if not found in    x not in y
"""

# Bitwise operators
"""
& 	AND	        Sets each bit to 1 if both bits are 1	                    x & y	
|	OR	        Sets each bit to 1 if one of two bits is 1	                x | y	
^	XOR	        Sets each bit to 1 if only one of two bits is 1	            x ^ y	
~	NOT	        Inverts all the bits	                                    ~x	
<<  left shift	Shift left by pushing zeros in from the right and let the leftmost bits fall off	                        x << 2	
>>	right shift	Shift right by pushing copies of the leftmost bit in from the left, and let the rightmost bits fall off	    x >> 2	  


